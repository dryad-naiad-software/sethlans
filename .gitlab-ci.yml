image: maven:3.3.9-jdk-8
cache:
  paths:
    - /root/.m2/repository/
before_script:
  - apt-get update -y && apt-get upgrade -y
  - apt-get install libc6:i386 libstdc++6:i386

build:
  tags:
    - linux
  script: "mvn package"
  
  artifacts:
    paths:
    - target/binaries/
